var express = require('express');
var router = express.Router();
var path = require('path');

/* GET users listing. */
router.post('/', function (req, res, next) { res.status(200) });

module.exports = router;